import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.bases',
  appName: 'bases',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
